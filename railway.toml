[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install"
watchPatterns = ["backend/**"]

[deploy]
startCommand = "cd backend && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[nixpacks]
configFile = "backend/nixpacks.toml"